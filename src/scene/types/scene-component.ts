import React from 'react';

/**
 * Type representing the interface of a scene component. I.e. a component that
 * would be a direct child of the scene manager.
 */
export type SceneComponent = React.FC;
