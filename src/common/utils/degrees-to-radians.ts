/**
 * Converts the give `degrees` to radians.
 * @param degrees The degrees to convert.
 */
const degreesToRadians = (degrees: number): number => degrees * (Math.PI / 180);

export default degreesToRadians;
